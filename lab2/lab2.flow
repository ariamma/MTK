import string;

main() {

    // Запросите у пользователя ввести данные
    //userInput = ();

    // ввести массив
    a = [0, 1, 2];
    sum = fold(a, 0, \x,y -> x+y);
    println(sum);
    quit(0);
}